name: Dev-app-Automatic-deployment

on:
  workflow_call: 
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: build process
      run: |
         echo "build process"
         run-name: test
  test:
    runs-on: ubuntu-latest
    steps:
    - name: test process
      run: |
         echo "test process"
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: deploy process
      run: |
         echo "deploy process"
